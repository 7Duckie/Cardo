# GitHub Actions CI for cardo.
#
# Runs on every push to main and on every pull request. Two jobs:
#   - lint:  ruff check across the codebase
#   - test:  smoke tests on Python 3.11, 3.12, 3.13 across Linux/macOS/Windows
#
# Smoke tests don't yet require a tests/ directory — they invoke cardo's
# subcommands against a temp directory and check exit codes. As the test
# suite grows in tests/, switch the `Smoke test` step to `pytest`.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # manual trigger from the Actions tab

# Cancel in-progress runs on the same branch when a new push lands.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install ruff
        run: pip install "ruff>=0.4"

      - name: ruff check
        run: ruff check cardo.py

      - name: ruff format --check
        run: ruff format --check cardo.py

  test:
    name: Test (Python ${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install cardo in editable mode with trash support so --trash
          # path code gets exercised.
          pip install -e ".[dev]"

      - name: Verify cardo is importable
        run: python -c "import cardo; print('cardo loaded OK')"

      - name: Verify --help works
        run: cardo --help

      - name: Read-only smoke tests
        run: |
          mkdir -p /tmp/cardo-smoke/sub
          echo "hello" > /tmp/cardo-smoke/a.txt
          echo "world" > /tmp/cardo-smoke/b.md
          echo "code"  > /tmp/cardo-smoke/sub/c.py
          python cardo.py stats /tmp/cardo-smoke
          python cardo.py tree /tmp/cardo-smoke
          python cardo.py search /tmp/cardo-smoke --min-size 1

      - name: Dry-run smoke tests (destructive commands, no real changes)
        run: |
          python cardo.py organize /tmp/cardo-smoke -r -n
          python cardo.py dedupe /tmp/cardo-smoke -r -n --mode quick
          python cardo.py clean /tmp/cardo-smoke -n
          python cardo.py rename /tmp/cardo-smoke --prefix "X_" -r -n 

      - name: Run pytest if tests/ exists
        shell: bash
        run: |
          if [ -d tests ]; then
            pytest tests/ -v
          else
            echo "No tests/ directory yet — skipping."
          fi
